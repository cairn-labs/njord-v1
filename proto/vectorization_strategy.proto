syntax = "proto3";

enum VectorizationStrategy {
  UNKNOWN_VECTORIZATION_STRATEGY = 0;
  FULL_ORDER_BOOK = 1;
  PRICE_ONLY = 2;
  PRICE_AND_VOLUME = 3;
  WEIGHTED_BAG_OF_WORDS = 4;
  STOCK_DATA_FRAME = 5;
}